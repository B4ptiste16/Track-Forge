AC BAPTOU — PROJECT HANDOFF (read this first in a new chat)
================================================================
(REBRANDED from "AC Track Forge" to "AC BAPTOU" in v0.8.0 — user's request.
productName/titles say AC BAPTOU; repo, appId and package name are UNCHANGED
so auto-update keeps working. Same app, now TWO sections behind a home page:)
1) TRACK BUILDER — design AC circuits: track mesh, kerbs, trackside strips,
   pit lane, buildings, AI line (fast_lane.ai); export FBX + config for AC.
2) AI TRAINING (v0.8.0, src/components/TrainCenter.tsx + rl:* IPC in
   electron/main.cjs) — in-app cockpit for the AC-RL python project
   (C:\Users\bapti\OneDrive\ac-rl, venv %LOCALAPPDATA%\ac-rl\venv):
   - lists AC tracks that have ai/fast_lane.ai
   - "Launch AC + Train" one click: swaps TRACK= in Documents\Assetto Corsa\
     cfg\race.ini, starts acs.exe, starts train.py; train.py WAITS for a live
     session (shared memory is all-zero until then) so order doesn't matter
   - graceful stop: app writes %LOCALAPPDATA%\ac-rl\stop.flag, trainer saves
     and exits on its own (30s later the app force-kills as backstop)
   - live telemetry from %LOCALAPPDATA%\ac-rl\live.json, model steps read
     from models/ac_sac.zip via jszip (cached by mtime), streamed stdout log
   - only whitelisted scripts can run (RL_SCRIPTS set), one process at a time

Stack: Electron (electron-builder NSIS, Windows ARM64) + electron-updater +
React + TypeScript + Vite + three.js (Z-up) + jszip. Custom ASCII FBX 7.4 writer.
Location: C:\Users\bapti\Documents\ac-track-forge
GitHub: https://github.com/B4ptiste16/Track-Forge   Current version: v0.10.0

BUILD / RELEASE:
- Version bumps: use a bump SCRIPT FILE (bump*.js), NOT `node -e "..."` — PowerShell
  mangles the quotes and builds the wrong version.
- Every change the user cares about = full build + push + GitHub release (they run
  from the installed app, not from source).
- KNOWN RECURRING ISSUE: Windows Defender quarantines the unsigned
  "AC Track Forge.exe" + ~8 DLLs (d3dcompiler_47, dxcompiler, dxil, ffmpeg,
  libEGL, libGLESv2, vk_swiftshader, vulkan-1) after EACH install. Workaround so
  far: copy them back from win-arm64-unpacked. User said on v0.7.2 "it still
  doesnt work but well pass for now" — app-open is still flaky, unresolved.

ARCHITECTURE — the geometry engine is the single source of truth:
- src/types.ts        all data shapes. Kerbs have per-part widths
                      (entryW/apexW/exitW) + unclamped apexLen. Trackside =
                      {left,right,zones} of StripCfg{texture,width,wall,wallDist}
                      with StripTexture grass|gravel|gravel_spaced|concrete.
                      Building{}, SavedLayout, pit.structures.
- src/state/project.ts  defaults, migration (legacy runoff -> zones), layout
                      snapshot/apply, color map.
- src/geometry/       kerbs.ts (tapered triangle ends, per-part widths),
                      runoff.ts (continuous trackside strip; smoothCeiling with
                      erosion-guarded blur to kill corner "wedge" artifacts),
                      build.ts (assembles mesh, mergeInto), pitlane.ts,
                      buildings.ts.
- src/export/ai.ts    genFastLaneAi() — rotates spline so point 0 = start/finish
                      line (fixes phantom ~3s laps + bot progress origin).
- src/components/     App.tsx (tabbed right panel: Track/Kerbs/Trackside/
                      Pit & Buildings + layout save/switch toolbar),
                      TracksidePanel, FacilitiesPanel, SegmentEditor2D
                      (modes: segment/zone/building). RunoffPanel was DELETED.

v0.10.0 (2026-07-21): auto-update re-checks every 30 min; trackside strips
+tarmac/+dirt (surfaces TARMAC/DIRT in surfaces.ini); kerb 'painted' (flush);
wall style 'hay'; Building.kind facades (offices/glass/brick/hangar -> meshes
DECOR_BUILDING/BLDGLASS/BLDBRICK/BLDHANGAR, one texture each); 2D editor:
draggable pane divider (App.tsx editFrac, localStorage), click-select segment
with inline R/angle/dir/length editor, 'lock rest' radius drags (adjusts the
two neighbouring straights so the corner's exit pose stays fixed - exact 2x2
solve in SegmentEditor2D onWindowMove, verified numerically).

STATE OF RECENT WORK (v0.7.0 -> v0.7.2):
- DONE: continuous chooseable-texture trackside strips; tapered/per-part/free
  kerbs; gravel_spaced (0.5m grass band); pit lane wall + boxes; buildings;
  alternate layouts with save UI; radically reworked tabbed UI; AI line starts
  at S/F line; timer/bot-progress measured from S/F.
- FIXED across 0.7.1/0.7.2: corner "weird zones"/wedge artifacts in the strip
  (strip stays continuous through corners; CLEARANCE 6->1.5; smoothCeiling blur
  guarded by erodeMin so razor-thin safety dips aren't averaged away).
- User's last word on wedges: "ITS still not perfect" — mostly good, not 100%.

LIKELY NEXT ITEMS IF USER RETURNS:
- The residual not-perfect corner strip smoothing.
- The app-won't-open / Defender quarantine problem (still unresolved).

Related project: AC-RL (the RL driver that consumes this app's fast_lane.ai).
See HANDOFF.txt in C:\Users\bapti\OneDrive\ac-rl.
